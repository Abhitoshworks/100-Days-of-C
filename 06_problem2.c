#include <stdio.h>
int main(){
    int r = 5;
    int h = 2;
    printf("the area of circle with radius %d is %f",r,3.14*r*r);
    printf("the volume of cylinder with radius %d height %d is %f" ,r,h,3.14*r*r*h);
    return 0;



}